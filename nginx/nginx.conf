events {
    worker_connections 1024;
}

http {
    # MIME types
    include /etc/nginx/mime.types;
    default_type application/octet-stream;

    # Logging
    access_log /var/log/nginx/access.log;
    error_log /var/log/nginx/error.log;

    # Cache storage configuration for tiles
    # levels=1:2 creates 2-level directory structure for better performance
    # keys_zone=tile_cache:10m creates 10MB zone in memory for index
    # max_size=1g allows up to 1GB on disk
    # inactive=7d removes files not accessed for 7 days
    proxy_cache_path /var/cache/nginx/tiles 
                     levels=1:2 
                     keys_zone=tile_cache:10m 
                     max_size=1g 
                     inactive=7d;

    server {
        listen 80;
        server_name _;

        # DNS resolver for Docker containers (allow resolution at request time)
        resolver 127.0.0.11 valid=10s;
        resolver_timeout 5s;

        # Define Martin backend address as a variable so DNS is resolved at request time
        set $martin_backend "http://martin:3001";

        # Health check endpoint (no cache, no logging)
        location /health {
            access_log off;
            return 200 "healthy\n";
            add_header Content-Type text/plain;
        }

        location ~ ^/tiles/([^/]+)/(0|[1-9]|1[0-2])/(\d+)/(\d+)(?:\.(pbf|json|png|webp))?$ {
            proxy_cache tile_cache;
            
            proxy_cache_valid 200 204 604800s;
            proxy_cache_valid 404 500 502 503 504 1s;
            
            proxy_cache_key "$scheme$request_method$host$request_uri";
            
            proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
            proxy_cache_background_update on;
            proxy_cache_lock on;
            
            proxy_pass $martin_backend/$1/$2/$3/$4;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            add_header X-Cache-Status $upstream_cache_status always;
            add_header X-Cache-Zoom-TTL "604800s (7-days)" always;
            
            add_header Access-Control-Allow-Origin "*" always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Access-Control-Allow-Headers "DNT, User-Agent, X-Requested-With, If-Modified-Since, Cache-Control, Content-Type, Range" always;
            
            proxy_buffering on;
            proxy_buffer_size 4k;
            proxy_buffers 8 4k;
        }

        location ~ ^/tiles/([^/]+)/(1[3-9]|2[0-9]|30)/(\d+)/(\d+)(?:\.(pbf|json|png|webp))?$ {
            proxy_cache tile_cache;
            
            proxy_cache_valid 200 204 86400s;
            proxy_cache_valid 404 500 502 503 504 1s;
            
            proxy_cache_key "$scheme$request_method$host$request_uri";
            
            proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
            proxy_cache_background_update on;
            proxy_cache_lock on;
            
            proxy_pass $martin_backend/$1/$2/$3/$4;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            add_header X-Cache-Status $upstream_cache_status always;
            add_header X-Cache-Zoom-TTL "86400s (1-day)" always;
            
            add_header Access-Control-Allow-Origin "*" always;
            add_header Access-Control-Allow-Methods "GET, OPTIONS" always;
            add_header Access-Control-Allow-Headers "DNT, User-Agent, X-Requested-With, If-Modified-Since, Cache-Control, Content-Type, Range" always;
            
            proxy_buffering on;
            proxy_buffer_size 4k;
            proxy_buffers 8 4k;
        }

        # OPTIONS request handler for CORS preflight
        location ~ ^/tiles/ {
            if ($request_method = 'OPTIONS') {
                add_header 'Access-Control-Allow-Origin' '*' always;
                add_header 'Access-Control-Allow-Methods' 'GET, OPTIONS' always;
                add_header 'Access-Control-Allow-Headers' 'DNT, User-Agent, X-Requested-With, If-Modified-Since, Cache-Control, Content-Type, Range' always;
                add_header 'Content-Length' '0' always;
                return 204;
            }
            return 405;
        }

        # Default route (404 for anything not matched)
        location / {
            return 404;
        }
    }
}
